[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    skill: marks tests related to skill validation
    script: marks tests related to script validation
    format: marks tests related to file format validation
